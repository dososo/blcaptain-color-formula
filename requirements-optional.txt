# 可选能力依赖；不装时仍可使用通用全局方向。
# 韩系清冷依赖人物保护，缺少依赖时必须阻断，不能回落无保护全局版本。
#
# 核心链路（inspect / suggest / plan / render / diagnose / palette / shots）
# 的通用全局路径只要 Python 3.9+ 与 ffmpeg、ffprobe。
# 韩系清冷最小额外依赖为 NumPy 与下列两个 macOS Python 绑定，不需要场景模型。
# 六套人工签名区域只需 NumPy、Pillow，不需要场景模型或自动跟踪后端。

# --- 人物类语义（person / face / salient_foreground / skin）---
# 仅 macOS 可用；调用系统自带的 Apple Vision，但需要 Python 绑定。
# 模型本身随 macOS 提供，不下载、不分发权重。
pyobjc-framework-Vision>=10.0 ; sys_platform == "darwin"
pyobjc-framework-Quartz>=10.0 ; sys_platform == "darwin"

# --- 场景类语义（sky / vegetation / building / product）---
# 实验能力：仅安装依赖不会下载模型，需要已有兼容模型缓存。
# 缺少缓存时 capabilities 明确显示不可用；不打包模型、不承诺冷安装场景 L2。
torch>=2.0
transformers>=4.35
numpy>=1.24
Pillow>=10.0
